apiVersion: v1
kind: ConfigMap
metadata:
  name: backend-config
  namespace: ecom
data:
  PORT: "5000"
  NODE_ENV: "production"
  # MONGO_URI now lives in the mongo-credentials Secret (it embeds the root
  # user/password) — see backend-deployment.yaml's env block.
